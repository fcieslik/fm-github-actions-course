name: CI

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - ".agents/**"
jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      node-version: "22"
      artifact-name: "build"
